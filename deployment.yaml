apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample_static-node-express-web-application
  labels:
    app: node-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-web
  template:
    metadata:
      labels:
        app: node-web
    spec:
      containers:
        - name: node-hello-container
          image: vikrammadhyasta/node:latest
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
